{
  "name": "credit-limit-validation-service",
  "private": true,
  "scripts": {
    "install": "pip install -r requirements.txt",
    "start": "python app.py",
    "test": "python test_service.py"
  }
}
